# Render Deployment Configuration

## Build Command
```
npm install && npm run build:server
```

## Start Command
```
npm run server
```

## Environment Variables
- `PORT`: 8787
- `NODE_ENV`: production

## Notes
The build command compiles TypeScript to JavaScript in the `dist-server` directory.
The start command runs the compiled JavaScript file.
